<h1>@ViewBag.title</h1>

<form method="post">
    @foreach (var photo in ViewBag.photos)
    {
    <input type="checkbox" name="cheeseIds" id="@photo.ID" value="@photo.ID" />
    <label for="@photo.ID">@photo.Name</label>
    <br />
    }    
    
    <input type="submit" value="Remove" />
</form>