

@model List<PhoMO.Models.PhotoDate>


@{
    ViewData["Title"] = "Date";
}


        <h1> Assortment of Dates</h1>
<h5>Dates correspond to the photos taken on each date.</h5>


  @if (Model.Count == 0)
        {
            <p>No Dates yet!</p>
        }
        <p><a asp-controller="Date" asp-action="Add">Add Date</a></p>

@using  (Html.BeginForm())
{

    <form asp-controller="Date" asp-action="ListByDate" method="post">
        Find by date: @Html.TextBox("DateString")
        <input type="submit" value="Date" />
    </form>


}
        <div>
            <ul>
                @foreach (var date in Model)
                {
                @*<tr>
                    <td>*@


                        @Html.DisplayFor(modelDate => date.DateTime)
                    <br/>
                    @*</td>
                </tr>*@
                @*<li>@date.DateTime</li>*@
                }
            </ul>
        </div>
